# ThreeAgent Real HTML Notes

These HTML fixtures prepare the GrantRound story for Website Parse, but they are not live settlement proof.

Before `ScreeningMode.ThreeAgent` is presented as the flagship live demo:

- Host real project HTML at public HTTPS URLs.
- Confirm Website Parse callbacks succeed against the hosted pages.
- Add a verifier workflow that combines JSON API facts, Website Parse facts, and LLM Inference.
- Keep judges as the only winner-selection authority.
- Keep Website Parse as enrichment, not payout authority.

Until then, use `ScreeningMode.TwoAgent` as the safe Somnia-powered fallback.
